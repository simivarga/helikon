# Fallback for `astro dev` only. `astro build` overwrites this file in dist/
# from SITE_URL and BASE_PATH (see robotsTxt() in astro.config.mjs), so the
# sitemap URL always matches the host the build is for.
User-agent: *
Allow: /

Sitemap: https://hotel-helikon.com/sitemap-index.xml
